
import PropTypes from 'prop-types';

const AllCraftItem = props => {
    return (
        <div>
            
        </div>
    );
};

AllCraftItem.propTypes = {
    
};

export default AllCraftItem;